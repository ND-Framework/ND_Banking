config = {
    valuesWithdrawATM = {20, 50, 100, 200, 500, 1000}, -- This is the values that can be withdrawn or deposited from an ATM.
    maxWithdrawDepositBank = 50000,
    minWithdrawDepositBank = 1000
}